# Push Code to New GitHub Repository

## Overview
Push the current codebase to the new GitHub repository at `https://github.com/samSignal/dsgate.git`.

## Current State
- **Current branch**: `Sam` (tracking `origin/Sam`)
- **Current remote**: `origin` → `https://github.com/samSignal/dgi.git`
- **Uncommitted changes**: 13 modified files + 6 untracked files
- **Branches**: `Sam`, `main`, `copilot/react-application-post-error-handling`

## Steps to Execute

### Step 1: Commit All Changes
```bash
# Stage all changes (modified + untracked files)
git add .

# Commit with descriptive message
git commit -m "feat: admissions workflow, offer letters, and admin improvements"
```

**Files to be committed:**
- Modified: 13 files (controllers, models, views, routes, config)
- Untracked: 6 files (new migrations, services, tests)

### Step 2: Rename Branch to Main
```bash
git branch -M main
```

### Step 3: Update Remote Origin
```bash
# Remove existing origin
git remote remove origin

# Add new origin
git remote add origin https://github.com/samSignal/dsgate.git
```

### Step 4: Push to New Repository
```bash
git push -u origin main
```

## Verification
- Confirm push was successful by checking the output
- Verify the remote URL is updated: `git remote -v`
- Verify branch is set to track origin/main: `git branch -vv`

## Notes
- This will disconnect from the old repository (`dgi.git`)
- All future pushes will go to the new repository (`dsgate.git`)
- The CRLF warnings are harmless and can be ignored
